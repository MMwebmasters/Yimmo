PORT=3000
NODE_ENV=production

# Clé secrète JWT — CHANGER en production (chaîne aléatoire longue)
JWT_SECRET=remplacer_par_une_cle_aleatoire_tres_longue_et_complexe

# Identifiants du compte administrateur
ADMIN_EMAIL=Yousri@yimmosarl.com
ADMIN_PASSWORD=VotreMotDePasseSecurise!

# Chemin de la base de données
# Sur Railway : /data/database.db  (volume monté sur /data)
# En local : laisser vide (utilisera database.db à la racine)
DB_PATH=
